# رمزنگاری سرتاسری حساب (E2EE)

رمزنگاری سرتاسری برای **کل حساب** از «تنظیمات حساب» فعال می‌شود و همه اطلاعات مالی را پیش از ارسال، در مرورگر رمزنگاری می‌کند:
پورتفوها (دارایی‌ها و تراکنش‌ها)، وام‌ها (با اقساط و پرداخت‌های اضافه)، درآمدها (همراه قواعد درآمد ثابت) و چک‌ها (با سوابق پیگیری).
سرور فقط متن رمزشده را ذخیره می‌کند و رمز عبور هرگز از مرورگر خارج نمی‌شود.

> [!CAUTION]
> **رمز عبور قابل بازیابی نیست.** اگر فراموش شود، داده‌های رمزنگاری‌شده برای همیشه از دست می‌روند.

---

## ۱. سلسله‌مراتب کلیدها

```text
رمز عبور ──PBKDF2 (SHA-256، ۱۰۰٬۰۰۰ دور، salt تصادفی)──▶ KEK
KEK ──باز می‌کند──▶ کلید داده حساب (تصادفی، ۲۵۶ بیت)          ← روی سرور فقط به‌صورت wrapped
کلید داده حساب ──باز می‌کند──▶ کلید هر پورتفو (تصادفی، ۲۵۶ بیت)  ← روی پورتفو به‌صورت wrapped
کلید داده حساب ──رمزنگاری──▶ رکوردهای وام، درآمد و چک
کلید هر پورتفو ──رمزنگاری──▶ دارایی‌ها و تراکنش‌های همان پورتفو
```

- همه رمزنگاری‌ها `AES-GCM-256` با IV تصادفی ۱۲ بایتی هستند، با قالب `enc:e2ee:v1:<Base64(IV + CipherText + Tag)>`.
- **تغییر رمز** فقط کلید داده حساب را با KEK جدید دوباره wrap می‌کند؛ هیچ داده‌ای دوباره رمزنگاری نمی‌شود.
- AES-GCM احراز اصالت دارد: رمز اشتباه هرگز به کلید «اشتباه ولی معتبر» نمی‌رسد و بازگشایی شکست می‌خورد.
- **کلید جدا برای هر پورتفو** اجازه می‌دهد یک پورتفو را بدون افشای بقیه اطلاعات به اشتراک گذاشت.

کد: `web/src/lib/e2ee.js` (توابع رمزنگاری) و `web/src/shared/vault/` (وضعیت، مهاجرت، ذخیره‌سازی).

## ۲. آنچه روی سرور ذخیره می‌شود

| داده | ذخیره‌سازی |
| :--- | :--- |
| salt و کلید داده wrapped | جدول `user_vaults` (وجود ردیف = رمزنگاری فعال) |
| وام‌ها، درآمدها، درآمدهای ثابت، چک‌ها، دارایی‌ها و تراکنش‌های پورتفو | جدول `vault_records`: `payload` رمزشده، به‌علاوه `record_date` (تاریخ اصلی، بدون رمز) و `parent_id` (برای دارایی و تراکنش: شناسه پورتفو) |
| کلید پورتفو | `portfolios.e2ee_wrapped_key` |

**استاندارد:** همه چیز رمز می‌شود جز **یک تاریخ اصلی** برای هر رکورد تا جستجو و فیلتر بازه زمانی سمت سرور ممکن باشد:

| نوع | تاریخ بدون رمز (`record_date`) |
| :--- | :--- |
| درآمد | تاریخ دریافت |
| چک | تاریخ سررسید |
| وام | تاریخ دریافت وام |
| درآمد ثابت | تاریخ شروع |
| دارایی پورتفو | تاریخ خرید |
| تراکنش پورتفو | تاریخ تراکنش (شمسی به میلادی تبدیل می‌شود) |

دارایی‌ها و تراکنش‌های پورتفو با **کلید همان پورتفو** رمز می‌شوند (نه کلید حساب)، پس لینک اشتراک که کلید پورتفو را در `#k=` دارد همچنان کار می‌کند. ردیف‌هایی که هنوز در جدول‌های قدیمی (`portfolio_holdings` و `transactions`) مانده‌اند، اولین باری که پورتفو باز شود به `vault_records` منتقل می‌شوند؛ هر انتقال ردیف قدیمی را در همان batch سرور حذف می‌کند. با غیرفعال‌سازی رمزنگاری، این رکوردها به‌صورت بدون رمز به همان جدول‌ها برمی‌گردند.

`GET /api/vault/records/:kind?from=YYYY-MM-DD&to=YYYY-MM-DD&parent=...` فقط روی همین فراداده فیلتر می‌کند. رکوردهایی که قبل از این تغییر رمز شده‌اند، دفعه بعد که خوانده شوند با همان متن رمزشده و تاریخشان دوباره ذخیره می‌شوند (بدون رمزنگاری دوباره).

**رمز نمی‌شوند:** همین تاریخ‌ها، نام پورتفوها، نام بانک‌های سفارشی، تعداد رکوردها و زمان‌های ایجاد/ویرایش.

وقتی رمزنگاری فعال است، سرور ساخت وام، درآمد، چک یا پورتفوی **بدون رمز** را با `409` رد می‌کند تا نسخه قدیمی برنامه داده را فاش نکند.

## ۳. محاسبات وام در مرورگر

سرور روی داده رمزشده نمی‌تواند محاسبه کند، پس برای حساب‌های رمزدار همه عملیات وام در مرورگر اجرا می‌شود:

- هر وام یک «سند» است: `{ loan, states, extraPayments }` که به‌صورت یک رکورد رمزشده ذخیره می‌شود.
- موتور `api/src/domain/loanDocument.js` (مشترک با وب) همان قواعد `loans.repository.js` را روی سند اجرا می‌کند.
- `loanApi.js`، `incomeApi.js` و `chequeApi.js` (در `features/`) امضای ثابت دارند و بسته به وضعیت رمزنگاری، درخواست را به REST یا به ذخیره رمزشده (`shared/vault/vaultLoans.js`، `vaultIncomes.js`، `vaultRecurringIncomes.js`، `vaultCheques.js`) می‌فرستند.
- اعتبارسنجی چک در هر دو مسیر همان `api/src/domain/chequeDocument.js` است.
- **تست تطابق** (`tests/unit/loanDocument.test.js`) سناریوهای یکسان را روی سرور و موتور اجرا می‌کند و نتیجه قسط‌به‌قسط باید یکی باشد.

## ۴. فعال‌سازی و غیرفعال‌سازی

**فعال‌سازی** (`encryptAccountData`):
1. کلید داده ساخته، با رمز wrap و در `user_vaults` ذخیره می‌شود.
2. برای هر پورتفوی عادی کلید جدید ساخته و **ابتدا** فلگ و کلید wrapped ذخیره می‌شود، سپس دارایی‌ها و تراکنش‌ها رمز می‌شوند.
3. هر وام (سند خام از `GET /api/loans/:id/document`)، هر درآمد و هر چک رمز و ذخیره می‌شود؛ سرور رکورد بدون رمز را در **همان batch** حذف می‌کند.

همه مراحل تکرارپذیرند؛ اگر وسط کار قطع شود، اجرای دوباره کار را تمام می‌کند و بخش «موارد رمزنگاری‌نشده» در تنظیمات آن را نشان می‌دهد.

**غیرفعال‌سازی** (`decryptAccountData`): همه رکوردها رمزگشایی و به جداول عادی برگردانده می‌شوند (`POST .../restore`، اتمی).
پورتفوها ابتدا از حالت E2EE خارج می‌شوند (کلید تا پایان نگه داشته می‌شود)، سپس اقلام رمزگشایی و در آخر کلید پاک می‌شود.
سرور حذف `user_vaults` را تا وقتی حتی یک مورد رمزشده باقی مانده باشد رد می‌کند.

## ۵. پورتفوهای رمزدار قدیمی (رمز جداگانه)

پورتفوهایی که پیش‌تر با رمز اختصاصی خودشان رمز شده‌اند (`e2ee_salt` و `e2ee_verifier`) **بدون تغییر کار می‌کنند**.
با فعال شدن رمزنگاری حساب، کلید همان پورتفو (مشتق از رمز قبلی) با کلید حساب wrap می‌شود؛ **به دارایی‌ها و تراکنش‌ها دست زده نمی‌شود**.
رمز قبلی از این منابع امتحان می‌شود: رمز جدید حساب، رمزی که در همین تب باز شده، یا رمزی که کاربر در تنظیمات وارد می‌کند.
تا آن زمان پورتفو با رمز خودش باز می‌شود.

## ۶. نشست و قفل

- پس از بازگشایی، کلید داده در `sessionStorage` همان تب نگه داشته می‌شود تا رفرش دوباره رمز نخواهد؛ با بستن تب، «قفل کردن» یا خروج پاک می‌شود.
- در حالت قفل، صفحه‌ها کارت بازگشایی نشان می‌دهند و هیچ داده‌ای در حافظه بارگذاری نمی‌شود.

## ۷. اشتراک‌گذاری پورتفوی رمزدار

لینک اشتراک، کلید همان پورتفو را در بخش `#k=...` دارد. مرورگرها این بخش را هرگز به سرور نمی‌فرستند؛
صفحه عمومی `/p/:slug` کلید را از لینک می‌خواند و داده را در مرورگر بیننده رمزگشایی می‌کند.
بدون لینک کامل، چیزی نمایش داده نمی‌شود.

## ۸. API

جزئیات در [API.md](API.md#end-to-end-encryption-vault-protected).
